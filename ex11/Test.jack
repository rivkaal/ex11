class Test {
    method void main() {
        var Array x,a;
        let x = Array.new(2);
        let a = Array.new(1);
        let x[1] = 2;
        let x[0] = 3;
        let a[0] = x[1];
        return;
    }
}